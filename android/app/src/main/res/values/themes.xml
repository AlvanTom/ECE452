<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ECE452" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/logo_green</item>
        <!-- Customize your theme here. -->
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.ECE452.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Set transparent background to hide launcher icon -->
        <item name="android:windowBackground">@android:color/transparent</item>
        
        <!-- Hide the action bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>